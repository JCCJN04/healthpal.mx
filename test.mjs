import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function check() { const {data: docs} = await supabase.from('documents').select('*'); const doc = docs[0]; const folder = doc.owner_id + '/' + doc.id; const {data: list} = await supabase.storage.from('documents').list(folder); console.log(list); const path = folder + '/' + list[0].name; const {data: s} = await supabase.storage.from('documents').createSignedUrl(path, 3600); console.log(s); } check();
